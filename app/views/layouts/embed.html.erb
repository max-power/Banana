<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= content_for?(:title) ? yield(:title) : "Activity" %></title>
    <%= stylesheet_link_tag "application" %>
  </head>
  <body class="embed-body">
    <%= yield %>
  </body>
</html>
